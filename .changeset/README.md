# Changesets

Run `pnpm run changeset` to record version bumps. The GitHub release workflow can consume the generated files to publish packages consistently.

For DataGrid packages, use the template:
`/Users/anton/Projects/affinio/.changeset/datagrid-template.md`
